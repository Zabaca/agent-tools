{
  "SessionStart": [{
    "matcher": "",
    "hooks": [{
      "type": "command",
      "command": "cd ${CLAUDE_PLUGIN_ROOT}/packages/google-cli && bun install --frozen-lockfile 2>/dev/null || true"
    }]
  }]
}
